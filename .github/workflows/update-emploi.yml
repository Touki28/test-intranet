name: 🔄 Mise à jour automatique de l'emploi du temps

on:
  schedule:
    - cron: '0 */6 * * *'  # Toutes les 6 heures
  workflow_dispatch:        # Permet de lancer manuellement

permissions:
  contents: write           # Permission d'écriture sur le dépôt

jobs:
  update-emploi:
    runs-on: ubuntu-latest
    
    steps:
      - name: 🛎️ Cloner le dépôt
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_PAT || github.token }}
          fetch-depth: 0
      
      - name: 📥 Télécharger le fichier ICS
        run: |
          curl -L -o emploi.ics "https://hplanning.univ-lehavre.fr/Telechargements/ical/Edt_INFOG2.ics?version=2022.0.5.0&idICal=44AB2688C2CC0095C8BC88B7F49766AD&param=643d5b312e2e36325d2666683d3126663d31"
          
          # Vérifier que le téléchargement a réussi
          if [ ! -s emploi.ics ]; then
            echo "❌ Erreur: le fichier téléchargé est vide"
            exit 1
          fi
          
          echo "✅ Fichier téléchargé avec succès ($(wc -l < emploi.ics) lignes)"
      
      - name: 💾 Commit et push si modification
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # Ajouter le fichier
          git add emploi.ics
          
          # Vérifier s'il y a des changements
          if git diff --staged --quiet; then
            echo "ℹ️ Aucune modification détectée"
          else
            echo "✨ Modifications détectées, création du commit..."
            git commit -m "🔄 Mise à jour automatique de l'emploi du temps

Fichier mis à jour le $(date +'%d/%m/%Y à %H:%M:%S')
Taille: $(wc -l < emploi.ics) lignes"
            
            echo "📤 Push vers le dépôt..."
            git push
            echo "✅ Mise à jour terminée avec succès!"
          fi
